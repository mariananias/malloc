#include <stdbool.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

int roundUpPower2(int i) {
  int original = i;
  while (i >= 2) {
    if (i == 2) {
      return original;
    } else
      i > 2
  }
}